format:
  inline: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  datatime: "%Y-%m-%d %H:%M:%S"

metadata:
  keys:
    - application
    - environment
    - host

logger:
  file_mode: a
  level: INFO
  verbose: False
  outputs:
    - stdout
    - file
  file_name: utils.log
